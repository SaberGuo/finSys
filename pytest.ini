[pytest]
addopts = -q --cov=finquant --cov-report=term-missing --cov-fail-under=80
testpaths = tests
python_files = test_*.py
markers =
    integration: marks integration tests requiring external dependencies or data
filterwarnings =
    ignore::DeprecationWarning
